export interface Icon {
  name: string;
  icon: string;
  url: string;
}

export interface LineMainInterface {
  span?: string;
  button?: string;
  line?: Users[];
}

export interface Users {
  name: string;
  img: string;
}
